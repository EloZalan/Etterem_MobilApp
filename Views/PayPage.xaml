<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WaiterApp.Views.PayPage"
             BackgroundColor="{StaticResource BackGroundGray}">
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="16">
            <Frame BackgroundColor="{StaticResource BackGroundGraylight}" >
                <VerticalStackLayout Spacing="8">
                    <Label Text="Payment" FontAttributes="Bold" FontSize="24" HorizontalOptions="Center" TextColor="White" />
                    <Label Text="{Binding CurrentOrderLabel}" HorizontalOptions="Center" />
                    <Label Text="{Binding StatusMessage}" HorizontalOptions="Center" TextColor="Gray" />
                </VerticalStackLayout>
            </Frame>

            <Frame BackgroundColor="{StaticResource BackGroundGraylight}" IsVisible="{Binding IsCashPaymentVisible}">
                <VerticalStackLayout Spacing="12" Padding="8">
                    <Label Text="Pay By Cash"
                           Margin="0,10,0,0"
                           HorizontalOptions="Center"
                           FontAttributes="Bold"
                           TextColor="White" />

                    <Button Text="Confirm Cash Payment"
                            HorizontalOptions="Center"
                            BackgroundColor="#009E60"
                            TextColor="White"
                            WidthRequest="300"
                            HeightRequest="80"
                            Command="{Binding PayCashCommand}" />
                </VerticalStackLayout>
            </Frame>

            <Frame BackgroundColor="{StaticResource BackGroundGraylight}" IsVisible="{Binding IsCardPaymentVisible}">
                <VerticalStackLayout Spacing="12" Padding="8">
                    <Label Text="Pay By Card"
                           HorizontalOptions="Center"
                           FontAttributes="Bold"
                           TextColor="White" />

                    <Label Text="Use the device NFC reader. As soon as any NFC card/tag is detected, the payment will complete."
                           HorizontalTextAlignment="Center"
                           TextColor="LightGray" />

                    <Button Text="Start NFC Card Payment"
                            HorizontalOptions="Center"
                            BackgroundColor="#1F51FF"
                            TextColor="White"
                            WidthRequest="300"
                            HeightRequest="80"
                            Command="{Binding PayCardCommand}" />
                </VerticalStackLayout>
            </Frame>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
