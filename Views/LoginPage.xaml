<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:tool="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="WaiterApp.Views.LoginPage"
             Title="Login">
    <ContentPage.Behaviors>
        <tool:EventToCommandBehavior Command="{Binding NavigatedToCommand}" EventName="NavigatedTo"/>
    </ContentPage.Behaviors>
    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="18" VerticalOptions="Center">
            <Label Text="Waiter Login"
                   FontSize="30"
                   FontAttributes="Bold" 
                   TextColor="{x:StaticResource Primary}"/>

            <Label Text="Sign in to manage your tables and orders."
                   TextColor="Gray" />

            <Entry Placeholder="Email"
                   Keyboard="Email"
                   Text="{Binding Email}" />

            <Entry Placeholder="Password"
                   IsPassword="True"
                   Text="{Binding Password}" />

            <Button Text="Login"
                    Command="{Binding LoginCommand}" 
                    BackgroundColor="{x:StaticResource Primary}"/>

            <ActivityIndicator IsRunning="{Binding IsBusy}"
                               IsVisible="{Binding IsBusy}" />

            <Label Text="{Binding ErrorMessage}"
                   TextColor="Red" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
